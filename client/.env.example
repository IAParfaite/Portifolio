# Client environment variables — copy to client/.env to override defaults.
# Default value is "/api" (uses Vite's dev proxy to http://localhost:5000).
# For a production build pointing at a deployed API, set the absolute URL, e.g.:
# VITE_API_BASE=https://api.yourdomain.com/api
VITE_API_BASE=/api